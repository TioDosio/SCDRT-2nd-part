- Critical sections em variaveis partilhadas;

- Pensar como fazer o stream de dados (mandar mensagem cada loop?);

- Implementar print do last minute buffer;

- Mandar msgs

- Hub

- Função para mandar uma mensagem geral (qualquer uma por input de entrada)

- se NÃO formos HUB quando recebemos um set processamos;
    se formos o HUB quando recebemos um set enviamos pelo serial;
 
- se NÃO formos o hub NÃO reencaminhamos as mensages que nao formos nós

- todos os Acks e Errs que o HUB recebe dá Serial.print -> Done, não testado 

- erros de envio de mensagem a serem mandados pelo can-bus para serem apresentados no Server -> Ver com alfa porque podemos ja ter


int counter;
loop{
    if time fire // 10ms
    {
        if (counsensus_off)
        {
        array[counter]
        if counter == 2
        {
            send_array()
            counter = -1;
        }
        counter++;
        }
    }
}